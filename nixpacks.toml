[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.install]
cmds = ["dotnet restore"]

[phases.build]
cmds = ["dotnet publish --no-restore -c Release -o /app/output"]

[start]
cmd = "dotnet /app/output/CountryCurrencyApi.dll --urls http://0.0.0.0:$PORT"